{"kind":"report","text":"Changes requested for exact head 9bdeacfa81bd4df21fbf567d3d59df1c2b87fba7 and its eleven artifacts. The split is sound and nearly done; a recut plus four bounded fixes remain. Independent gates at the head, all green: gofmt, git diff --check, go vet, go test and go test -race in the root module and in jsonataddl with GOWORK=off (16 packages ok, 0 FAIL), go mod tidy leaves both modules unchanged, govulncheck v1.7.0 finds nothing in either module, scripts/demo.sh passes, tree clean, branch equals origin. Boundary: GOWORK=off go list -deps ./... and -deps -test in jsonataddl/ return no Tailapps package outside jsonataddl. Root consumes the module through replace =\u003e ./jsonataddl with no duplicate implementation; internal/profile keeps only facade files. Consumer proof reproduced independently: I built a file:// module proxy from the head (zip, .mod, .info, list for v0.1.0) and ran scripts/verify-jsonataddl-module.sh v0.1.0 against it from a clean temp module with no replace or workspace; it resolved the exact version, compiled and printed effective. Behaviour: outside tests, docs, corpus, go.mod/go.sum, LICENSE and NOTICE, the only jsonataddl/ change is two doc comments in dialect.go; no exported API change. LICENSE and NOTICE are byte-identical to root. Relocated host tests differ from their a79901f sources only in package clause and one fixture path. Required: 0. Stale base, per hugh assert d4ba47e4 which I acknowledge: the head sole parent is a79901f while main is a7e4270, and the exact head release.yml still writes release assets to dist inside the checkout, reverting the v0.1.4 clean-metadata repair in the reviewed tree. A three-way merge (git merge-tree, clean) would keep main RUNNER_TEMP lines, but the exact head must itself carry the repaired workflow: recut onto a7e4270, keep the module gate lines, and re-run the release dry-run at the new head. 1. Coverage regression (request: relocate tests while retaining their coverage). jsonataddl/corpus_test.go:24 replaces profile.RuntimeID with the literal tailapp-otlp-1.8-json-v1-ddl-jsonata-v206-sqlite-3.53.4@5, so the goldens no longer pin the host constant at internal/profile/types.go:17; compiler_test.go:102 and runtime_test.go:35 compare against the constant itself and no other test holds the literal. That constant selects the legacy compiler and upgrade path in internal/engine/engine.go:193, so an accidental edit would silently reclassify existing projections. Add one assertion in internal/profile pinning RuntimeID to the exact literal, cross-referencing the corpus goldens. 2. Verifier strength (request: dependency and security checks and a clean external consumer proof; docs/reference/dependency-security.md:12 claims sum.golang.org verification). scripts/verify-jsonataddl-module.sh pins GOWORK and GOPROXY but inherits GOFLAGS, GOPRIVATE, GONOPROXY, GONOSUMDB and GOSUMDB; with GOPRIVATE=github.com/generalbusiness-ai set, which I hit on my first run, it bypasses the proxy and the checksum database and still prints verified. Set GOFLAGS= GOPRIVATE= GONOPROXY= GONOSUMDB= GOSUMDB=sum.golang.org on each env invocation. Should-fix: 3. Root go.mod:6 requires jsonataddl v0.1.0, a tag that exists only after the merge; replace hides it locally, but go install of the root module from outside the checkout and any root release tag cut before jsonataddl/v0.1.0 will fail with unknown revision. The request already orders the tag after the merge; record in jsonataddl/README.md that jsonataddl/v0.1.0 must be pushed before any later root release tag. 4. .github/workflows/jsonataddl-release.yml:23 uses git describe --exact-match --tags HEAD, which returns one tag when several point at HEAD (reproduced with jsonataddl/v0.1.0 and v0.1.5 on one commit), so a module release co-located with a root tag can fail the gate; use git tag --points-at HEAD | grep -Fxq \"$GITHUB_REF_NAME\". Nits: the boundary grep in ci.yml:47 and jsonataddl-release.yml:42 matches only /internal/ and omits -test, narrower than its message; jsonataddl/README.md:5 and :63-71 reference notes and internal/profile that do not exist in the published module, and :88-105 mix module-directory and repo-root commands; the verifier duplicates example_test.go; notes/2026-08-28-jsonata-ddl-core-extraction.md:276-286 still frames the module location as open. Architecture: a module boundary placed at the existing package boundary; docs/reference/architecture.md untouched by the diff and the architecture note updated in the same head; the host-specific Tailapp() dialect stays exported from the host-neutral module with its consistency proof on the host side, mitigated by the release gate requiring ancestry of origin/main. Security: no defect in the module; default-deny authorizer, bounds, confinement and value codec are byte-identical to a79901f; workflows use $GITHUB_REF_NAME as a shell variable, not expression interpolation; the verifier cleanup is prefix-guarded; item 0 is a reintroduced release-metadata defect in the exact head and item 2 is a verification-strength gap. Simplification: the verifier could exercise the packaged example instead of a second copy of the same program, and the boundary grep can be one anchored pattern.","body":{"artifact":"git:sha1:da732b0bdaad4426ed4ad666b892d8a7c68f625f#git:sha1:75f7b8acb0eaa6e0b817b4072938de98f54d8c4d","head":"9bdeacfa81bd4df21fbf567d3d59df1c2b87fba7","head_news_acknowledged":"[\"git:sha1:da732b0bdaad4426ed4ad666b892d8a7c68f625f#git:sha1:af8a385a4b0ef39228d334f4d7f2c1471045dd4d\",\"git:sha1:da732b0bdaad4426ed4ad666b892d8a7c68f625f#git:sha1:b29bf2bdc7502acd31c49e110c9fbae0d4ba47e4\"]","review_frontier":"git:sha1:da732b0bdaad4426ed4ad666b892d8a7c68f625f#git:sha1:b29bf2bdc7502acd31c49e110c9fbae0d4ba47e4","review_path":"reviewguard@1","verdict":"changes-requested"}}
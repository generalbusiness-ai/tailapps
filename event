{"kind":"promise","text":"Builder promises extraction stage 6: remove the duplicated legacy implementation and the differential scaffolding now that corpus, differential, activation, upgrade, race, and end-to-end tests all pass. The legacy resolver remains but re-seats on the core compiler under the legacy identity seed (the differential corpus proved digest- and diagnostic-identical behavior); the fold-input read conversion moves into the core unchanged in shape; and every live fold read executes under the core's default-deny authorizer, closing the last completion criterion. No semantic change to live behavior; the composed identity stays as pinned. Work branches off main 90b3fb978715e07f9605ae4d70df7da87834d30c; closed only by an independently checker-approved merge."}
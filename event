{"kind":"request","text":"Upgrade gap closure: release-consuming upgrade, ingestion_ready-aware status, machine-readable result, Linux, and rollback tests","body":{"conditions":"PROVENANCE: operator direction, hugh, 2026-08-31 (gap closure for #2272 per checker audit; depends on the setup gap-closure request). DELIVER at a reviewed exact head, against the #2272 conditions verbatim: the upgrade surface stages a VERIFIED RELEASE (checksum and signature, same verification as the installer) in a versioned user-owned directory, retains the known-good prior binary, atomically switches the stable current target, restarts the user service (launchd and systemd --user), and rolls back if control-plane startup fails; it distinguishes a healthy control plane from ingestion_ready - a runtime-profile mismatch must be reported prominently as an expected app-upgrade requirement with the exact next commands, never treated as a failed binary upgrade and never auto-reactivating apps; by default it installs no bundles, rewrites no source, activates nothing, resets nothing, changes no telemetry config; a machine-readable upgrade/status result (JSON) with clear manual next commands; tests for failed-start rollback and the upgrade-pending case. Keep the existing macOS script working or supersede it explicitly. GATES: full battery, demo, sh -n; per-file full-SHA artifacts; review to checker against the #2272 conditions.","to":"00721374a520eabbbd4ded64dd018110f47545ff20112bee83c75f478e15f818"}}
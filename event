{"kind":"request","text":"README: add a short, data-directed prompt agents can use to produce an HTML analysis report with charts from their Tailapp results","body":{"conditions":"PROVENANCE: operator direction, hugh, 2026-08-31, relayed verbatim by checker: \"task a readme update that specifies a short prompt for agents to produce a similar report from their available data; prompt is: use the data results to produce a html page with interesting charts - but that prompt could use some explicit direction based on available data elements.\" Rests on the live README at main c43e7b3 and on the operator sanction assert a2b70848 that names the reference report reports/daily-review-analysis-2026-08-30.html (operator-run spike; local, untracked, scrubbed). DELIVERABLE: a README section (or a short docs page linked from README, whichever keeps README compact) giving one copy-pasteable prompt an agent connected over MCP (or a person with the CLI) can run to produce a report like the reference. The prompt must stay SHORT (a paragraph, not a spec) but add explicit direction grounded in the actual exported data elements at this head: name the tables to query (daily_review; activity-stats event_inventory and the session/performance exports; session_cost; agent-guard telemetry_coverage/session_progress and policy exports; signal-counts) and the dimensions they carry (harness, day_utc, record/failure/slow-event counts, token families incl. cached and reasoning, cost_microusd, latency buckets, outcome rates, tool/endpoint buckets, repeated-failure and no-progress evidence, presence-only privacy indicators). It must tell the agent to (a) state each chart scope honestly - complete-day snapshot vs cumulative totals - and surface projection completeness/position and any ineffective or stalled coverage rather than silently mixing frontiers, exactly as the reference report does; (b) keep it privacy-preserving: aggregates only, no session ids, paths, commands, prompts, or URLs; (c) produce a single self-contained HTML file with inline charts (no external scripts) that leads with two or three headline numbers and ends with an evidence-and-next-questions section citing positions. Show the resulting section in the README near What the results look like or Documentation, keep the value-first ordering, and keep all links valid. Optionally commit a scrubbed example report under docs/ (operator did not ask for this; if you do, say so and confirm it contains no identifiers). CONSTRAINTS: docs-only change; go test ./..., go vet ./..., go test -race ./... pass at the exact head; verify every table and column named in the prompt exists in the shipped bundles at that head (checker will diff the prompt against the application.sql exports). Address the review request to checker at an exact committed head.","to":"00721374a520eabbbd4ded64dd018110f47545ff20112bee83c75f478e15f818"}}
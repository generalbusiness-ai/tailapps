{"kind":"request","text":"Operator-directed normalizer follow-up (observed against the live resident 2026-08-28): extend the Codex service-identity normalization to cover an additional observed identity. All three bundled normalizers currently map only event.source = \"codex_cli_rs\" or \"codex_exec\" to harness \"codex\" (single line, identical in agent-guard/session-cost/activity-stats folds/normalize.jsonata). The running service shows a session_cost row under harness \"codex-app-server\" (1 session, 0 tokens) that is NOT folded into \"codex\" — so Codex telemetry fragments across an extra harness value in every per-harness rollup. Requested: (1) audit the Codex OTLP service.name values the real harness emits (the CLI already gave codex_cli_rs and codex_exec; codex-app-server is now observed; confirm whether there are others such as a plain codex or a TUI/app-server variant) and decide the canonical set that must normalize to \"codex\"; (2) add codex-app-server (and any other confirmed Codex identity) to the source-\u003eharness mapping consistently in all three normalizers, keeping codex_cli_rs and codex_exec; (3) cover it with a scrubbed compatibility fixture and a test asserting the new identity maps to harness=codex, mirroring the earlier codex_exec live-aliases work; (4) note in the session-cost/README that a normalization change does NOT retro-relabel already-materialized rows — pre-change rows persist under their old harness value because continue-activation preserves tables (this is why stale codex_exec rows remain in a long-running projection); a reset would relabel but discards history, so document it rather than forcing a reset. Out of scope: no schema change, no reset of existing projections. Follow the usual flow: implement on a branch, publish exact-head artifacts, request checker review.","body":{"conditions":"At an exact reviewed head: all three bundled normalizers (agent-guard, session-cost, activity-stats) normalize the confirmed additional Codex service identity codex-app-server (and any other Codex service.name values the audit confirms) to harness \"codex\" while preserving the existing codex_cli_rs and codex_exec mappings; a scrubbed compatibility fixture plus a test assert the new identity yields harness=codex; the session-cost documentation notes that already-materialized rows keep their old harness label after a normalization change (continue-activation preserves tables); no writable-table schema change and no projection reset; the full go test ./..., go test -race ./..., go vet ./..., and ./scripts/demo.sh gates pass; builder publishes exact-head artifacts for the changed paths and files a review request to checker, and a checker review verdict is filed on that head.","to":"00721374a520eabbbd4ded64dd018110f47545ff20112bee83c75f478e15f818"}}
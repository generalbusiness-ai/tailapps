{"kind":"request","text":"Setup gap closure: released POSIX installer flags, in-binary setup operation, service-manager fallbacks, canonical home resolution, and mocked tests","body":{"conditions":"PROVENANCE: operator direction, hugh, 2026-08-31 (gap closure for #2271 per checker audit; depends on the release gap-closure request). DELIVER at a reviewed exact head, against the #2271 conditions verbatim: the installer/setup surface defaults to installing all five built-in bundles and supports --bundles LIST, --bundles none, and --interactive only when /dev/tty is available with no prompt by default; an in-binary setup operation (for example tailapp setup) that installs only requested missing built-ins through the normal control path and never overwrites or activates an existing Tailapp; user-owned paths only, never sudo, never edits harness telemetry settings; macOS LaunchAgent and Linux systemd --user unit on the same canonical home, start, wait for control-plane health, status/log guidance; Linux lingering attempted when authorized, otherwise an explicit statement that persistence ends at logout with the administrator request; when no usable user service manager exists, retain binary and bundles, report persistence incomplete with an exact foreground/supervisor remedy and a nonzero result unless --no-service; resolve the documented-home vs os.UserConfigDir discrepancy in code and docs; test installer decisions with mocked platform/service boundaries. Keep scripts/setup-resident.sh as the source-checkout path or fold it into the released installer, but say which. GATES: full battery, demo, sh -n; per-file full-SHA artifacts; review to checker against the #2271 conditions.","to":"00721374a520eabbbd4ded64dd018110f47545ff20112bee83c75f478e15f818"}}
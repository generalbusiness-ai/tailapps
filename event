{"kind":"assert","text":"Operator directs the next Tailapp change: add bounded self-observability for active-use performance measurement. Expose CLI and MCP metric snapshots for intake, backpressure, per-Tailapp processing, queries, errors, and fixed-bucket latency distributions; use low-cardinality dimensions, include no telemetry payload data, and defer optional OTLP self-export so the resident cannot recursively ingest its own metrics."}
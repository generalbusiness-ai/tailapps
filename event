{"kind":"assert","text":"Operator direction, hugh, 2026-08-31, relayed verbatim by checker, refining #2193 (README agent-report prompt) and informing #2189 (richer local dimensions): on the reference report reports/daily-review-analysis-2026-08-30.html, \"the timeline view is particularly good, but additionally would benefit from per-session bar breakdown.\" The timeline in question is the When-and-how-much-arrived / Coverage-within-the-day chart (per-harness activity across 00-24 UTC with partial-day and stopped-emitting annotations). FOR #2193: the README prompt should explicitly direct agents to include (a) that per-harness arrival timeline with honest coverage annotations, and (b) a per-session breakdown drawn as bars: one bar per session spanning first-seen to last-seen on the same time axis, grouped or colored by harness, with magnitude (event_count, or tokens/cost when joined) shown by bar height or label. At this head that is answerable from shipped exports without new engine work: activity-stats session_activity (harness, session_id, event_count, first_seen_unix_nano, last_seen_unix_nano, tool/prompt/response/request/success/failure counts) and agent-guard session_progress (first/last activity, repeat and no-progress counts), joinable to session_cost on (harness, session_id). The prompt must remind the agent these session exports are CUMULATIVE since activation (not day-bucketed) and to label the bar chart scope accordingly, and to keep session identity aggregate-safe (bars labeled by harness and ordinal or hashed short prefix, never raw session_id in the page). FOR #2189: hourly or day-bucketed per-session volume (session x time bucket) is not answerable today; if the richer-dimension work adds session label/id-prefix retention, consider a bounded per-session time-bucket breakdown as a candidate dimension so the timeline bars can show intra-session activity, not just span. Not a new request; builder should fold this into the existing two commitments and cite this assert in the reports."}
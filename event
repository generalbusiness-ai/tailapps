{"kind":"request","text":"Start the url-capture feature set: the URL-level loopback web-reputation pipeline, design-first then staged implementation","body":{"conditions":"PROVENANCE: operator direction, hugh, 2026-08-31, relayed verbatim by checker: \"ok to request builder start the url-capture feature set\". This lifts the discussion-only status of notes/2026-08-30-web-domain-reputation.md (live artifact 36decf15 at merge a7c52eda, latest note revision 651dc83, approved by checker 7b460a80) into tasked work. SCOPE is the loopback design as merged: one Tailapp with three OTLP-flavored input families (URL observations carrying the fetched URL as a DEDICATED attribute, never the guard-shared target attribute; exclusion records materializing an exclusions table on top of no-configuration defaults for loopback/private-range literals, localhost, single-label, .local/.internal/.home.arpa/.test; url-reputation verdict records emitted back by the assessing agent), so that unchecked = observed minus excluded minus fresh-verdict is an ordinary query with no side-cache; plus daily-review cron instructions and an EXAMPLE companion script that reads the unchecked list, applies the exclusions a second time before egress, queries Google Safe Browsing or VirusTotal, and emits url-reputation OTLP records to the resident. The scan layer stays instructions-plus-script: no engine code and no fold logic for scanning. STAGE 1 (deliver first, for checker review before any implementation): a design-decision note under notes/ that resolves the six open questions of the merged note with a concrete recommended answer and rationale each. OQ1 capture route and the reputation-service choice/licensing (Safe Browsing non-commercial vs Web Risk; VirusTotal public-API terms) are OPERATOR decisions: present each as explicit options with a recommended default and mark them for operator ratification rather than deciding silently. OQ2 must state the private-event and table shapes and name the single writing program per table under the two-stage topology and single-writer rules. OQ3 verdict freshness, OQ4 companion-script home/schema/key handling, OQ5 retention and reset semantics, OQ6 whether the script strips path-and-query secrets before transmission (recommendation: yes by default, with the precision cost stated). The stage-1 note must also include the staged implementation plan (suggested: stage 2 Tailapp source set with fixtures and embed tests; stage 3 companion script plus cron instructions with a scrubbed fixture end-to-end; stage 4 docs/catalog integration including README catalog row and privacy statement) with each stage independently reviewable at an exact head. CONSTRAINTS carried forward unchanged: existing RuntimeID/revision digests unaffected until a stage explicitly says otherwise; no new confinement holes; privacy posture change (full URL retention locally, transmission only of selected non-excluded URLs) stated plainly in user-facing docs; existing bundles untouched except explicit catalog listing. Address the stage-1 review request to checker at an exact committed head; go test ./..., go vet ./..., go test -race ./... must pass at every reviewed head.","to":"00721374a520eabbbd4ded64dd018110f47545ff20112bee83c75f478e15f818"}}
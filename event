{"kind":"request","text":"url-capture Stage 2: build the url-reputation built-in Tailapp exactly as specified by the merged, ratified stage-1 decision note","body":{"conditions":"PROVENANCE: operator direction, hugh, 2026-08-31: \"ensure that URL-capture work is progressed.\" Stage 1 is complete and merged on main 56730a8 (notes/2026-08-31-url-reputation-stage-1-decisions.md, ratified per assert c9c7fa4c, approved 52181cec), whose status line says stage 2 may begin; nothing for stage 2 exists yet (no request, no promise, no tailapps/url-reputation, no scripts/url-*). This request opens Stage 2 under umbrella #2187 (9bf357c0). SCOPE, from the note section Stage 2: Tailapp source: add the url-reputation built-in bundle with the three-family normalizer (tailapp.url.observed / tailapp.url.exclusion / tailapp.url.reputation log records as specified in section 2, with their required and optional attributes, exclusion kinds host-exact/host-suffix/url-prefix, verdicts clean/suspected/error), the normalizer as sole writer of url_observations (PK observed_full), url_exclusions (PK exclusion_id; built-in IDs for loopback/private literals, localhost, single-label, .local, .internal, .home.arpa, .test seeded only when absent), and url_verdicts (PK observed_full+provider), a single counts fold writing url_pipeline_counts (PK day_utc+event_family), explicit exports for all four tables, the host-consistency refusal (re-parse observed_full; refuse when tailapp.url.host disagrees with the parsed lowercase host), and the documented bounded due query (observations whose host matches no enabled exclusion and whose provider has no verdict with valid_until_unix_nano \u003e :now_unix_nano; now supplied by the caller). Add fixtures for observed, exclusion, clean, suspected, error, expiry, disabled-rule, built-in-exclusion, missing-field, and host-mismatch records. Add embed/compile, normalization, fold, query (due query with a bound now), continue-compatibility, and bounded-performance tests. Register the bundle in tailapps/embed.go (Names, Load) and in the cli.md/mcp.md bundle lists; write tailapps/url-reputation/README.md (input model, tables, due-query recipe, retention/reset statement incl. re-emitting custom exclusions after reset, privacy posture: full URLs retained locally, egress only via the stage-3 companion). CONSTRAINTS from the ratified plan: do not change RuntimeID, canonicalization, evaluator confinement, storage, transport, or any existing bundle; freshness lives in verdict rows (checked_unix_nano, valid_until_unix_nano), never in a wall clock read by a program. Stage 3 (scripts/url-capture adapter plus scripts/url-reputation companion: Safe Browsing v5 default under per-installation eligibility confirmation, Web Risk fallback, VirusTotal Premium-only, mandatory egress scrub) and stage 4 (catalog and daily-review instructions) follow as separate requests. GATES: go test ./..., go vet ./..., go test -race ./..., scripts/demo.sh at the exact head; per-file full-SHA artifacts; review request to checker at an exact committed head - I will verify the DDL against the note (single-writer tables, key shapes, exports), run the fixtures through normalizer and fold, execute the due query with a bound now against a live install, and confirm no existing bundle or runtime identity changed. BOOKKEEPING: the stale rows #2531, #2528, #2527, #2518 and my #2686 are satisfied by merged successors (README prompt via abb6014, URL note via 56730a8, composition via a838bfc); supersede or close them, and re-report umbrellas #2524 and #2187 at their merged heads.","to":"00721374a520eabbbd4ded64dd018110f47545ff20112bee83c75f478e15f818"}}
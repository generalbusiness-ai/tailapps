{"kind":"assert","text":"Implementation note for the live jsonataddl module request: a nested go.mod will make root `go test ./...` and the current CI/release workflows stop traversing jsonataddl automatically. The delivery must therefore give the root module an explicit local dependency on the nested module, run the nested module's test/race/vet/vulnerability gates explicitly in CI, and keep the external-consumer proof free of replace/workspace directives. Move the two parent-internal integration tests to the root side rather than letting the published module's test graph depend back on the Tailapps parent module. The v0.1.4 advance from a79901f to a7e4270 changed only .github/workflows/release.yml and did not change jsonataddl."}
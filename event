{"kind":"report","text":"CHECKER VERDICT (as report; guarded review blocked by short artifact commit IDs) for scripts/setup-resident.sh at exact commit d385c972535b6f278ca67d1964b515708d626a09: CHANGES REQUESTED for one safety gap. Verified: clean worktree go vet OK, go test 14 ok, go test -race 14 ok, scripts/demo.sh exit 0, sh -n clean, 33 markdown files link-clean; scripts/build.sh present; tailapp init idempotent on an existing home. Flow audit good: loopback-only --otlp, absolute --home, XML/whitespace guard, versioned binary with overwrite refusal, atomic link rename, staged plist with plutil -lint, staged systemd unit, honest loginctl linger failure; mutation boundary holds (only create-only apps install for bundles absent from apps list). Empirical on this macOS host running the standard LaunchAgent (read-only invocations): --dry-run against the matching service exits 0 with the four would-lines; --dry-run --home /tmp/other-home refused exit 65; --otlp localhost:4318 refused exit 64. REQUIRED R1: replace_link renames a new symlink over whatever is at ~/.local/bin/tailapp; if that is a regular file (exactly what the README manual go build path produces) it is silently destroyed. Mirror the upgrade script: refuse with exit 65 when $bin_link exists and is not a symlink with an absolute target, and document it. ADVISORIES: A1 re-running against a healthy matching service kickstart -k restarts a working resident (brief intake interruption) - skip when the domain is loaded and the link target is unchanged, or document the restart; A2 link the Upgrading an existing resident procedure for the runtime-identity hold once merged; A3 build_id requires a git checkout at --source; A4 run shellcheck in CI for both scripts."}
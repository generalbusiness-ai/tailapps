{"kind":"request","text":"Expand harness coverage: Kiro CLI (v3 focus), Kiro application, and VS Code, where each supports telemetry","body":{"conditions":"PROVENANCE: operator direction, hugh, 2026-08-31, relayed verbatim by checker: \"expand the harness coverage to include: Kiro CLI (note there is a v3 version available which is generally much more capable than previous, so focus on that if docs are available), and Kiro (application) and VS Code if they support telemetry.\" Rests on the live harness guide index docs/harnesses/README.md at main c43e7b3. SCOPE: three candidate harnesses, investigated and delivered in the same shape as the existing Claude Code, Codex, OpenCode, and Pi coverage. (1) KIRO CLI: focus on the v3 line; locate its official telemetry/OTLP documentation, determine what it emits (logs, spans, metric points; service.name identity; event names; token/cost/tool/API attributes; content gates), and deliver a docs/harnesses/kiro-cli.md guide (send telemetry + MCP connection + current bundle fit + security, matching the other guides), normalizer recognition in the shipped bundles that apply (activity-stats, agent-guard, session-cost, daily-review, signal-counts where relevant) gated on the observed service identity exactly as OpenCode is, a scrubbed OTLP fixture, and embed/normalizer tests. If v3 docs are not available, say so with evidence and cover the documented version, stating the version gap plainly. (2) KIRO APPLICATION (the IDE): determine from official documentation whether it exposes telemetry an operator can point at a local OTLP/HTTP receiver. If yes, same deliverables as (1). If it only has product telemetry to the vendor, or no configurable export, deliver a short evidence-backed negative finding in the harness index (what was checked, with citations) rather than a guide. (3) VS CODE: same investigation. Distinguish VS Code product telemetry (vendor-bound, not user-routable) from extension-level telemetry that could be exported over OTLP (for example an OpenTelemetry-emitting AI/agent extension, or Copilot-adjacent tooling); if a routable OTLP path exists, cover it; if not, record the negative finding with citations. DO NOT invent event vocabularies: every recognized event name and attribute must be traceable to official docs or to a captured scrubbed fixture, and unknown coverage must remain honest (agent-guard target_coverage unknown where detail is gated) exactly as for Claude Code. Every new harness identity mapping must be exhaustive per normalizer (all three or more normalizers updated together, as the OpenCode correction established) and must not alter existing harness recognition. CONSTRAINTS: docs match the value-first README conventions (README catalog and harness index updated; links valid); no new dependencies; go test ./..., go vet ./..., go test -race ./..., and scripts/demo.sh pass at every reviewed head; RuntimeID/revision digests of existing bundles change only if a normalizer changes, and if so say so in the report. DELIVERY: may be one head or staged per harness; address each review request to checker at an exact committed head. Stage first, if useful, an evidence note summarizing what each of the three actually supports before writing normalizers, so operator can redirect early.","to":"00721374a520eabbbd4ded64dd018110f47545ff20112bee83c75f478e15f818"}}
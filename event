{"kind":"request","text":"README sample preview adjustment: commit the checker-generated visual report HTML as the sample, link to it (not to an image), show a Sample: header with a vertically truncated crop from When-and-how-much-arrived through about ten timeline rows, and make the image link to the HTML","body":{"conditions":"PROVENANCE: operator direction, hugh, 2026-08-31, relayed verbatim by checker: \"adjustment to the result preview: use your visual-report-2026-08-31.html, and link to that html (not to an image); readme to include an image: a vertically-truncated portion from when and how much... through part of the timeline view (~10 rows of timeline data); readme image links to the html report; small header above the readme image: Sample:\". Rests on the live README artifact on current main. SOURCE: the checker-generated report is at reports/visual-report-2026-08-31.html in the repo working tree (untracked; produced by running the README prompt against the live resident; generator reports/build-visual-report.py, also untracked). It is self-contained (inline SVG and script, no external resources) and my scan found zero raw session IDs, UUIDs, filesystem paths, prompts, or tool arguments - sessions are labelled harness plus ordinal and a 6-hex hash; it does contain real cumulative cost figures and model names, which the operator has accepted for publication. Re-run that scan before committing. DELIVERABLE: (1) copy the HTML unchanged to docs/assets/visual-report-sample.html (do not regenerate it). (2) In README under Make a visual report, replace the current sample paragraph with: a text link targeting the HTML, e.g. [sample result](docs/assets/visual-report-sample.html) - the link must NOT target an image; then a small header line reading exactly Sample: (bold line or #### heading, whichever keeps the section compact); then the image wrapped in a link to the same HTML: \u003ca href=\"docs/assets/visual-report-sample.html\"\u003e\u003cimg src=\"docs/assets/visual-report-sample.png\" alt=\"Sample visual report, truncated: arrival timeline with per-session bars\" width=\"...\"\u003e\u003c/a\u003e. (3) Regenerate docs/assets/visual-report-sample.png from the NEW HTML: render at 1280 px wide with headless Chrome, then crop vertically so the image starts at the When and how much arrived heading and ends after roughly ten rows of the timeline (scope paragraph plus about ten session bars; each timeline row is 18 px in the SVG viewBox, so roughly 560-620 px tall at 1280 wide - verify by eye); PNG optimized, well under 500 KB; record the exact Chrome command and crop offsets in the commit message. (4) Keep the PNG path so existing links do not break. GATES: README.md, docs/assets/visual-report-sample.html, docs/assets/visual-report-sample.png are the only changes; all local links and href/src references resolve; go test ./..., go vet ./..., go test -race ./... at the exact head; per-file full-SHA artifacts for all three; address the review request to checker - I will diff the committed HTML against my file byte for byte, re-run the privacy scan, and check crop bounds, size, alt text, header text, and both link targets.","to":"00721374a520eabbbd4ded64dd018110f47545ff20112bee83c75f478e15f818"}}
{"kind":"promise","text":"Implementing MCP stage 5 now on request/mcp-stage5-skill off merged main a04c9ee8: the harness findings still reward it (Claude Code, Codex, and OpenCode all load SKILL.md skills, and OpenCode reads both other harnesses' locations), so a compact cross-harness skill is generated from the same embedded content as the overview resource, byte-derivation tested, emitted only by an explicit documented command, with per-harness placement documented. Stage 4 follows as its own focused delivery."}
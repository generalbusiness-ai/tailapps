URL observation host-binding correction under request4086 and ordinary adopted decision5060faff, ratified228032ac. Corrects accepted Checker readiness920e631c and Hugh finding61e91aba. Exact head f4f9ef0c38844956aafa21135cd3cfd7343afedf from current main e3ad5f92; four changed paths only.

Architecture: preserves the application-semantics, typed normalizer/analytic pipeline, and host boundaries in [the architecture](notes/2026-08-28-tailapp-architecture.md@f4f9ef0c38844956aafa21135cd3cfd7343afedf:94). This is a correction of the bundle's existing actual-host input condition. No engine/core/runtime/schema/dependency/table/export change, and no claim about activation or continuation eligibility.

Security: compares the actual authority host after valid optional userinfo; only a numeric port outside IPv6 brackets may follow. Preserves the original observed URL exactly, including local userinfo/query/fragment. Rejects mismatches, prefix lookalikes, multiple raw @ signs in the authority, invalid userinfo escapes, whitespace/control/backslash authorities, empty hosts, malformed IPv6 and unbracketed IPv6. Tests include all five reproduced cases, DNS/Unicode/punycode/IPv4, all IPv6 compression positions and IPv4 tails, plus long userinfo/path within the existing input ceiling. Refusals are ineffective and eventless.

Simplification: the existing allowlist has no authority split or substring-before/after function, no lambdas/ranges/recursion. The bounded 18-step binary search uses only existing contains/substring/length/floor and object/path expressions; each step keeps the half containing the first @. Since input is at most 256 KiB, after 18 steps one character remains. It runs only when an @ occurs before /, ? or #. Failure to locate it refuses. No capability bypass, builtin shadowing, new parser/library or global validator refactor. The IPv6 expression covers compressed/uncompressed 128-bit forms, optional IPv4 tail and encoded zone; the numeric suffix inside brackets remains part of the host.

Validation: all 11 required normal local CI checks pass with Go1.26.7 on this clean exact head, including both module tests/race/vet, boundary/public-core selection, both vulnerability checks and demo. The new evaluator regression tests fail with the original normalizer supplied through a Go overlay; source was never replaced for that control. See checks.json and before-control.log. Docs describe the corrected source. No resident, install, activation/reset, hook, provider or release operation.

Inherited ordinary staleness is acknowledged as in the request and intake promise. The former source/test/Stage1 note bytes matched current main before this correction; conditions, performer, destination and ordinary adopted authority remain unchanged. No authority-bearing relay-chain substitution. Full I7 resumes after this bounded delivery; Kiro remains after full I7; URL Stage3+4 stays after both.

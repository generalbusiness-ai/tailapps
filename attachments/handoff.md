# Refresh the I7 decision on the implemented input contract

Tailapp source `7ecffd53013fd6ca45693a1a8e28b7c8d52432e8` contains the input validation that the existing I7 decision still calls a proposed API. Keep the existing capability/context design and all eight admission gates. Refresh its baseline and integration details so implementation starts with a current, reviewed decision.

The [existing decision](notes/2026-09-05-capability-context-contract.md@e1ab955fc3ea5473fdc47dab8ccc380087da07a0) remains useful. Its merged artifact 418fb2f10a5cd307101046840b66e65e6edee4a7 is stale; its old commissioned chain cannot meet the current freshness condition for beginning follow-on work. The Gitseq adoption proposal 17250 remains adopted and supports the overall direction. This refresh will use the ordinary proposal-and-ratification path for the revised detailed decision, before independent review and delivery. It does not revive the satisfied original design commission.

Current integration facts:

- [Input validation](jsonataddl/input.go@7ecffd53013fd6ca45693a1a8e28b7c8d52432e8:10) now runs before host reads and repeats with rows during Evaluate. Preserve both checks. The eventual extension session's Prepare must add verified context before reads and its Evaluate must consume the prepared handle once.
- [Input contracts](jsonataddl/input_contract.go@7ecffd53013fd6ca45693a1a8e28b7c8d52432e8:9) have four closed forms. Base metadata permits scalars only. The proposed reserved context slot belongs in compiled per-program metadata, with caller context rejected; do not silently make the host's base metadata open. Bounded arrays/unions, metering and provider contracts are still future work.
- [Identity](jsonataddl/identity.go@7ecffd53013fd6ca45693a1a8e28b7c8d52432e8:22) still has nine components. The full I7 plan adds the tenth and changes dialect/API/evaluator identity. Preserve explicit projection upgrade/reset requirements and the already implemented stored-runtime guard.
- [Projection orchestration](internal/projection/projection.go@7ecffd53013fd6ca45693a1a8e28b7c8d52432e8:518) validates inputs before reads. Its gap persistence still stores the ordinary error string, so the I7 plan's inner, outer and persistence-boundary secret sanitation requirements remain necessary. This read is a future extension integration requirement, not evidence of a current extension leak.
- The same nineteen built-ins and JSONata 599f35f32e5f / SQLite 0.35.3 pins remain. The AST callback still does not prove all builtin work or allocation bounds. Keep the metered evaluator patch, codec coverage, physical memory proof, immutable reviewed pin and rejecting omission controls. No timer-only substitute or new uncontrolled dependency.
- Chess now has source b97c6a82ef7e3618721696f5a69efef13da10a79 but retains Gitseq host 7152e79a741e and notnil/chess 1.10.0. Its whole-log identity resolution and raw Method().String() remain unadmitted by I7. Keep the exact-record identity, scope, expiry and revocation comparisons and the later separate Chess adoption gate.

The seven current source paths in [the comparison](source-comparison.json) are byte-identical to their live c57f34b2 artifacts, but those artifacts are historically stale through retired engine basis 4761b410. That is evidence to reconcile, not a reason to erase staleness by relabeling old artifacts. Publish current exact source bases only with an explicit explanation of the changed engine/upgrade context; repair only the provenance needed by this decision and preserve all other live candidates.

The revised note should retain the four delivery stages: meter/core, host/release, providers/exact-record context, then Chess adoption. Make all eight gates and their positive/omission controls traceable. A changed head needs a fresh independent review. No provider, runtime, release or live-service operation belongs to this documentation delivery.

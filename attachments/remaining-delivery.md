# URL reputation: finish the remaining source delivery

Implementation handoff governed by the ordinary Hugh proposal carrying this attachment, once ratified. Corrected readiness report 920e631c confirms the host-binding defect; the report has been accepted. The separate normalizer repair must land before companion implementation.

The Stage 2 bundle is delivered. The remaining outcome is a usable, optional capture-to-reputation workflow: Stage 3 producers and end-to-end proof, followed by Stage 4 discovery and operator instructions. Queue the implementation after Builder completes the full I7 Stage 1 request #4043; the small MCP guidance correction #4065 and already-promised Kiro assessment #4055 retain their existing scheduling commitments.

## Authority and current bases

The carrying ordinary Hugh proposal and its ratification govern the remaining stages, based on the current published Stage 1 decision and corrected readiness report. Do not treat the historical Checker relay c9c7fa4c as a Hugh ratification. Explicitly reconcile inherited ordinary staleness against current main and unchanged source bytes. Preserve source-only scope: no actual hook installation or trust action, provider account or network operation, scheduler, resident upgrade, activation or reset.

The governing source note is `notes/2026-08-31-url-reputation-stage-1-decisions.md`; the current Stage 2 interface is `tailapps/url-reputation/README.md`, `tailapps/url-reputation/application.sql`, `tailapps/url-reputation/folds/normalize.jsonata` and `tailapps/url-reputation/folds/pipeline.jsonata`. Final filing must provide an owning source head and verify actual file previews for every reference.

## Stage 3: capture and enrichment

Ship `scripts/url-capture/` with a documented stdin contract and separate installation profiles for Claude Code, named Codex local/MCP tools and OpenCode v2. Accept only supported, fixture-backed successful web-fetch tool results. Preserve exact locally observed HTTP(S) URLs and trustworthy parsed hosts; emit known redirects separately, retain supplied harness/session identity and state unknowns honestly. Unknown tools/shapes and undocumented WebSearch result forms produce explicit missing-coverage reporting, not guesses. Codex hosted WebSearch is unsupported by local hooks; non-managed hooks require explicit definition trust. Never read transcripts, session databases or paths supplied by hook records.

Build against the separately corrected bundle normalizer. Its delivered host-binding contract must preserve exact local userinfo-bearing URLs, compare their actual host correctly, reject spoofed supplied hosts (including colon-before-@ cases), and preserve legitimate numeric ports and bracketed IPv6 literals. The companion independently parses and validates URL/host agreement before exclusions or egress; it must not trust a stale or malformed stored host blindly. Do not introduce an engine, runtime, transport or schema extension.

Ship `scripts/url-reputation/` to query due observations and current exclusions through supported local interfaces. Apply built-in private/loopback and symbolic rules plus enabled exact-host, host-suffix and URL-prefix rules per original observation. Parse and independently validate host identity before exclusions or egress. Strip userinfo, fragment and the entire query, and redact recognized path secrets by default; the explicit path-preserving alternative still strips userinfo/query/fragment. No full-query egress option.

Require one selected provider, its installation eligibility confirmation and its environment-only credential before any provider request. Retain the existing Safe Browsing non-commercial default, Web Risk for uncertain/commercial use, and licensed VirusTotal Premium-only override. Refuse missing settings; no automatic fallback. Keep credentials out of CLI/config/logs/OTLP and scrub provider request URLs and failures before emitting error telemetry.

Bound input/output sizes, batches, concurrency, rate handling and retries. Group eligible observations by scrubbed checked_full, reuse unexpired provider results from existing verdict rows across new original URL forms, and emit one verdict per exact eligible original. Apply current exclusions before both reuse and egress. Safe Browsing result expressions require proper matching and fanout, and returned cacheDuration applies to all queried URLs. Web Risk matches use returned expiry; empty clean Lookup responses use bounded local fallback TTL, not Update API negativeExpireTime. Preserve configured fallback and short error-retry bounds in the decision. Use the existing loopback OTLP/HTTP JSON interface and stored verdict timestamps, without a new cache or database.

Prove each supported hook fixture through emitted OTLP and normalization to an exact observation row, then a disposable loopback end-to-end path through due query, exclusions, scrubbed deduplicated fake-provider lookup, reputation OTLP and a fresh verdict. Include cache reuse across runs/new originals, provider expression matching, expiry/error behavior and negative egress/error-leak controls. Fake-provider and fixture capture identities must be distinguishable from real providers and harness capture. Do not exercise real accounts, hooks, provider credentials or the real telemetry resident.

## Stage 4: make the delivered workflow discoverable

Reconcile the built-in catalog and install documentation with the already-embedded sixth bundle. Keep url-reputation opt-in; catalog inclusion does not authorize changing setup defaults or activating a resident. Update daily-review instructions with due queries, companion invocation, qualified suspected-hit reporting and required attribution, explicit capture coverage, reset/no-replay caveats and restoration of custom exclusions. Provide concise first-encounter MCP resource links to the bundle and companion. Document hook trust and pinned supported profiles, refresh the Web Risk reference, and correct the decision note's authority/status wording to cite the new ordinary adoption. Preserve existing bundles, exports and runtime compatibility.

## Delivery

The single assigned outcome covers both remaining stages after the separate normalizer repair. Deliver the companion and catalog/review documentation together. Do not close this request after Stage 3 while leaving Stage 4 as prose saying later. Use an isolated request branch/worktree, exact-path artifacts grounded in current behavior, independent Checker review with architecture/security/simplification conclusions, approval ratification, sealed merge, source and sequence push, applicable CI and owned branch/worktree cleanup. Verify source links in the actual preview surface. Fixture/loopback success proves source delivery only, not actual installation or live provider coverage.

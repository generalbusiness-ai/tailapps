# Tailapp recovery preparation for request3914

Prepared 2026-09-06, observations10:55–10:59UTC. This completes preparation only. No live process, link, definition, projection or queue was changed, and no live database was copied.

## Verified deployment and condition

PID66700 loads inode304121714 at `/Users/hughpyle/.local/lib/tailapp/tailapp-20260831T183703Z-5d84ac71d625`, also the target of `/Users/hughpyle/.local/bin/tailapp`. SHA256: `5e500c92db3aa7990bc803884e631e4d2b3f0008bd75f0d1b6d9cf5f8d0bc7c9`. Go1.27.0, darwin/arm64 v8.0, CGO_ENABLED=1; source revision5d84ac71d625b29d9e7e9dbdf4c7c66c60429434, vcs.modified=true. The embedded Pending symbol names the original `/Users/hughpyle/play/tailapp/internal/inbox/inbox.go`. LaunchAgent `ai.generalbusiness.tailapp` has KeepAlive=true and executes the stable link with `serve --otlp-http 127.0.0.1:4318`; home is `/Users/hughpyle/.local/share/tailapp`.

At10:55:54 the control database held delivery head1,511,679, queued records87,531 and queued bytes183,888,723. All six applications had87,531 pending obligations each, positions1,424,149–1,511,679; total525,186. Every physical projection matched its registered active revision and runtime `jsonata-ddl-runtime:sha256:5032bcfe6634db4462fcfe39775e55d1c7e11fcb7663c961f6ebe8860192b8b1`; all interpreted positions were1,424,148, complete=true and no gap. The attached inventory records every full revision, draft, activation boundary and schema. No activation journal or pending mutation was present.

Apps_list completed in13ms; status timed out at10.009s; metrics completed in7.565s. The later metrics sample had87,512 records/525,072 pending obligations and successful processing in all six applications, zero unavailable/upgrade-pending apps and zero detached obligations. This is slow progress, not a stopped worker. CPU was99.6%. These are separately timed read-only metadata observations, not a consistent whole-home backup or a claim that every counter is simultaneous. Raw telemetry was neither selected nor exported.

## Source choice and proposed delivery

All21 external modules in the loaded build match the historical go.sum, including JSONata599f35f32e5f, SQLiteGo0.35.3 and SQLiteWasm3.2.35304. The attached build-input inventory contains every module/version/sum, Go settings, historical go.mod and source stamp. The source stamp is `https://github.com/generalbusiness-ai/tailapps`; no explicit version stamp is recorded. Historical scripts/build.sh supplies that URL through ldflags and runs go build ./cmd/tailapp.

The dirty main-module inputs remain unidentified. Main reflog confirms the5d84 merge two minutes before installation; current source/notes/reports and the bounded72-event installation-window record reveal no captured patch or source manifest. No other Tailapp worktree remains. This search does not prove that no external archive exists. Non-Go/embedded inputs and the exact dirty diff are not recoverable from module checksums or matching runtime IDs alone. A clean5d84 checkout is therefore a reference, not the deployed source, and no compatible replacement binary has been built or selected.

Preferred separately assigned implementation: recover and commit the full deployed source first, then branch `request/recover-5d84-pending-query` from that identified commit. Change only Pending ordering from e.position to the equal o.position, with the bounded-index and control-responsiveness tests backported without dependency/runtime/schema changes. Retain Go1.27.0, the recorded build flags and21 pinned dependencies, plus the identified main-module and embedded inputs. Independently review the exact source and binary; publish its complete build manifest/hash. Proposed versioned destination is `/Users/hughpyle/.local/lib/tailapp/tailapp-recovery-3914`; refuse if it already exists. Its source parent and SHA256 are intentionally unresolved gates, not invented values. Implementation and switch need separate bounded requests.

If the dirty inputs cannot be recovered, the smallest fully identified source alternative is delivered main7ecffd53013fd6ca45693a1a8e28b7c8d52432e8 with its committed go.mod/go.sum and public jsonataddl0.2.0. Its pinned runtime is `jsonata-ddl-runtime:sha256:3e60ce38390fa376d766b5880cb8f79ebd9196f0e4036335beb9285783efbd33`, versus deployed5032. Source recovery keeps historical projections queryable but upgrade-pending; binary replacement cannot promise to drain their captured old-revision obligations. Their rows and queue must remain intact. Owner acceptance of query-only containment is a separate choice; reset, detachment and historical replay are not included. Do not switch just to obtain a healthy socket. Retaining the current binary while resolving provenance remains the default.

## Consistent preservation and switch gates

1. Before any authorized switch, identify and pause all upstream exporters with confirmed retry/spooling behavior; prevent new CLI/MCP mutations. Only the Tailapp LaunchAgent was found in the bounded relevant-agent inventory, which does not identify all emitting clients. Record intake settings and prove delivery head stops advancing after in-flight intake finishes. This missing exporter inventory must be resolved by the operator; pausing without it cannot promise telemetry preservation.
2. Arrange launchd to prevent KeepAlive restart, then request graceful SIGTERM shutdown. Record the prior job configuration and disabled state for restoration. Verify the process actually exits and no process holds writable database files. The historical close waits for the worker without a deadline, so the10s HTTP shutdown budget is not proof of process exit. A timeout is a stop condition requiring a separate operator decision; forced termination must not be labelled clean shutdown. Do not use the ordinary kickstart upgrade script as a substitute for this staged preservation procedure.
3. With all writers stopped, make a permission-preserving whole-home copy to `/Users/hughpyle/.local/share/tailapp-recovery-3914/baseline`, including control.sqlite, every projection database, any remaining WAL/SHM files, definitions/revisions and durable metadata. Preserve the exact original executable, stable-link target, previous-link target and plist beside it. Current home size is about504MB; reserve at least2GB for baseline, disposable verification and post-write retention, then recalculate before copying. Socket files are transient and should be absent after clean shutdown; any exception must be inventoried. No live per-database backup, immutable=1 read or file-by-file live copy counts as a whole-home snapshot.
4. Compare file manifests and SHA256 hashes before/after the stopped copy; keep baseline immutable to the procedure. Run SQLite quick_check on disposable copies, not by modifying baseline. Save exact active/draft revisions, stored runtimes, schemas, table row counts and private local content digests, delivery head, obligation identities/states/revisions, projection frontiers, gaps and durable counters. Definition and telemetry content stays local. A copied control database alone cannot prove projection preservation.
5. Start only the reviewed candidate on a second disposable home, isolated from production intake/control and exporters. Require no activation, detachment, reset or identity rewrite. Compare pre-write state to baseline; during draining require monotonic frontiers, constant delivery head with intake off, exact captured revision delivery, falling pending obligations and no new gaps/detached work. Compare deterministic projected output with an original-binary run on a separate equivalent disposable copy where feasible. Verify status/schema/metrics responsiveness and zero runtime-upgrade gating for the backport. Full source provenance remains necessary even if selected behavioral comparisons pass.
6. Only after requester acceptance of those results, a separately authorized switch may preserve the stopped live home, atomically repoint the stable link to the hashed candidate and restore the LaunchAgent. Recheck loaded inode/hash, active revisions/runtime, queue/frontier conservation and draining. Resume intake only after acceptance; retain baseline until the owner accepts recovery.

Rollback before candidate writes restores the original recorded link and verified unchanged home. After writes, stop intake and candidate, preserve a second consistent whole-home snapshot, and assess original-binary compatibility with those writes before restart. A link reversal does not reverse data. Restoring baseline would discard any post-baseline accepted telemetry/projection progress unless separately retained/reconciled; it requires explicit owner authorization and an accounting of that interval. The existing tailapp.previous link points to abb60147, not the currently loaded5d84 binary, so it is not the recovery rollback target.

## Handoff

The preparation is verifiable and complete; source reconstruction, exporter quiescence, cold-copy preservation, candidate build/testing and live switch are still gated. No compatible binary, successful backup, restart or recovery is claimed. Hugh/authorized Planner can ratify this report and commission the smallest next outcome after choosing the source path. The delivered resident-upgrade guide at7ecffd53 governs these boundaries.

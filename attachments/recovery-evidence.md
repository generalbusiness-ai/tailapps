# Compatible Pending backport evidence

Exact candidate f36cb0eb2645cce40ebd30466406d0f293e6b455 starts from accepted deployed-program source 5d84ac71d625b29d9e7e9dbdf4c7c66c60429434. It owes delivery to refs/heads/recovery/resident-5d84-pending, not main. The only production input change orders the existing join by o.position; selected fields, revision, FIFO, limit validation and settlement are preserved. Test fixtures and their manifest are public historical sources and synthetic records.

All 1,974 compiler input hashes across 405 packages were compared with the accepted reference manifest. Only internal/inbox/inbox.go differs. All five recorded Go tool hashes match; module pins and go.mod/go.sum are unchanged. Runtime identity remains 5032bcfe6634db4462fcfe39775e55d1c7e11fcb7663c961f6ebe8860192b8b1. The six actual deployed definition digests are asserted after ordinary compilation, with each source set recovered from Git history. No identities are rewritten and no live telemetry is copied.

The actual Pending call passes mixed-consumer/revision/state and limit controls. The 87,531-row/six-consumer query comparison proves equal ordered selected rows at limits1 and1024. Limit1 uses32 VM steps/no sort, versus875352 steps/one sort for the historical query. Restoring e.position in the actual production query causes the regression gate to fail. Full go test -race ./... passes, including inbox and real control integration; go vet ./... and go mod verify pass.

The resident fixture initializes all six exact historical definitions, seeds87,531 synthetic records with525,186 obligations, and performs two start/drain/stop cycles. Status/schema/metrics remain responsive. Exact definitions, physical projection identities, runtime, frontiers and queue conservation survive restart. The observed ordinary run reached1,600 consumed positions per app with85,931 pending each and zero incorrect revisions, duplicate progress, reset or detached obligations. This is bounded progress and restart evidence, not a full-backlog drain claim or live-home compatibility snapshot.

Three builds reproduce the candidate bytes, including a separate checkout and fresh cache. Candidate SHA256 fcb54bb4476a1be4a4f14eefb89f6b74616cd7d54109310894389a64381461ae. Exact commands, environment, source archive, tool and input manifests are retained here. Builds use -trimpath and clean VCS stamping; those metadata choices differ from the historical installed dirty build. The previous qualified reference-program finding remains the bridge to that installed binary; no exact historical dirty-file manifest is claimed.

A controlled clean baseline build with the same flags permits a direct comparison. All10,039,156 bytes of machine text are equal after replacing the83-byte build ID. The only read-only program-data difference after VCS revision/time/module-version normalization is the single e-to-o query character. Other executable data/type/GOT/stub sections match. All23,697 function layouts and2,509,785 instruction addresses retain their source-file associations; changed PC line numbers are confined to inbox.go. All609,000 decoded DWARF entries retain structure and attributes except source line numbers and the consequent14-byte line-table offset shift. The7 changed module descriptor words each move by8 bytes, matching the reduced PC-line table. Section comparison, decoded metadata and mutation-rejection controls are retained. Linker UUID/signature and compressed debug-container bytes are build metadata; this evidence does not claim byte identity with the installed original.

The non-main succession plan must accompany review and be checked again after approval. Hugh specifically requires preserving current main inbox publication #3906, even though the current classifier calls it abandoned against the recovery destination. That finding is Gitseq #19530; it does not authorize cleanup of another destination or commission a classifier change. No binary installation, resident replacement, launchd/intake action, live database read/copy/export or schema migration has been done. Separately accepted cold-backup/WAL, quiescence and rollback gates remain required for any later live operation.

--- v206/functions.go
+++ v206/functions.go
@@ -2890,6 +2890,8 @@
 		if len(args) > 1 && args[1] != nil {
 			if p, ok := args[1].(float64); ok {
 				val := int(p)
+				// Probe only: Intel masked CVTTSD2SI NaN integer-indefinite result.
+				if math.IsNaN(p) { val = -9223372036854775808 }
 				precision = &val
 			} else if p, ok := args[1].(int); ok {
 				precision = &p

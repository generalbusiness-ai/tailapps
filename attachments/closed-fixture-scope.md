# Disposable closed-host fixture for I7

Source: evaluator bebeed31322f96ce9d75044acd71382466fde5c2 archived into an owned temporary directory, with one added proof-only file v206/i7_closed_profile_probe.go. The actual private compiler/evaluator is unchanged. A separate main module declares Go1.26.7 and uses a temporary local replacement; this is not a public evaluator API, dependency pin, final host or adopted runtime profile.

The program prepares five constant sources once, then performs100 rounds of each (500 evaluations): true, sum, regex contains, lexical builtin rebinding and partial substring rebinding. Each result is compared with its fixed expected JSON and both depth counters must unwind to zero. The first fixture used Work=2^24 and refused sum with resource_work. The retained diagnostic confirms that cause. The final fixture uses the existing semantic-corpus allowances Work=2^44 and Allocation=2^40, Depth128. No public limit is inferred from either fixture allowance.

All four Go1.26.7 CGO0 binaries compiled (Darwin/Linux ARM64/AMD64). Only Darwin ARM64 executed, with GOMAXPROCS=1 set in that subprocess, and GODEBUG/GOGC/GOMEMLIMIT absent; it passed all500 evaluations. The main-module version is26.7, not the dependency module's older compatibility version. Build records, exact source addition, program and binary hashes, failed-attempt logs and final success log are retained in this directory. No resident or user's process setting changed.

I inspected nm and all-binary disassembly on all four final binaries. Each has fourteen direct calls in the named runtime creation/accounting/retirement families, plus five self-retry jumps after stack-growth checks. The nineteen instruction records must not be reported as nineteen distinct goroutine creations. The calls are all in runtime: cleanup/finalizer/background GC/startup/update helpers, newproc wrappers, newproc1/allgadd, mexit, a retained extra-M path and debugger wrapper. No WaitGroup.Go, time.goFunc, iter.Pull/Pull2, newcoro, synctestRun, StartTrace or traceStartReadCPU symbol is present. The debugger wrappers and oneNewExtraM remain linked; absence of their ordinary invocation still needs the explicit no-debugger/no-foreign-callback premise. An instruction scan alone is not a proof of all indirect targets or operating-system control transfers.

This is a concrete, small program on which to discharge host assumptions, in addition to the earlier production-dependency source inventory. It does not show that arbitrary hosts or providers have H=0, that a dynamic-P host satisfies the fixed-P lemma, or that repeated production events have a bounded prior-retention baseline. The500 executions are consistency evidence, not a memory measurement or all-input proof. Preparation/acquisition, complete runtime/allocator/stack and retained-metadata composition, provider fixture and complete core obligations remain open on4043. No full F or admission is claimed.

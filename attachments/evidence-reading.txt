Source-only request4252; no runtime admission or public module publication. Start with numeric-evidence-summary.json, then numeric-conversion-inventory.json. The34 floating conversions are assessed individually; typed-conversions.json preserves every208 integer-target site across63 production files. No grep-only reachability claim.
To repeat evidence, extract replay-numeric-evidence.py and omissions-test.go into the same new directory, then run:
python3 replay-numeric-evidence.py PRIVATE_CHECKOUT_AT_311d9afc CORE_JSONATADDL_AT_62fde NEW_OUTPUT_DIRECTORY
The core module must retain pinned599f; Go1.26.7 on ARM64 reproduces the recorded native-cast survival. The script writes only experiment files, uses Go overlays, and never changes tracked runtime source. Its native results and private logs preserve outputs/errors, ordered builtin dispatch and actual bound outcomes. It expects intentional nonzero test exits from five controlled mutations; ARM native-cast restoration explicitly survives. No AMD64 evaluator execution is claimed.
Run conversion-inventory.go with GOWORK=off CGO_ENABLED=0 go run from the private evaluator root to repeat go/types scanning. Reachability conclusions in the inventory are separate from that syntactic/type census. Legacy matcher conversions reachable through contains were assessed explicitly; private contains removes unused integer bookkeeping while preserving its type predicate.
The separate native-group probe uses pinned599f and input[1,2]; the protected-slice tests check fatal-vs-ordinary grouping behavior and refusal before native bounds panic.
Earlier Builder numeric evidence is signed at4236/4239; Hugh independently supplies four-target compiler/native evidence at retired4249, linked from live4252. Compiler instructions and Intel SDM inference remain distinct from runtime execution. Instruction summary included here retains its exact earlier private source/compiler basis; current311d changes only layout constraints since the later runtime/package head.
All listed costs remain provisional logical resource units; complete final physical mapping and selected-provider/host replay/cross-target gates stay on4043. The note records language/error requirements rather than claiming those gates are complete.
